# RasoiAI Camera — RPi dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
google-genai>=1.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Only available on Raspberry Pi — install separately:
# sudo apt install python3-rpi-lgpio
# pip install RPi.GPIO
